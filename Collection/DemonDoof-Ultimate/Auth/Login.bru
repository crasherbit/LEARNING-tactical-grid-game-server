meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}/api/v1/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email":"{{randomEmail}}",
    "password":"{{randomPassword}}"
  }
}

vars:post-response {
  JWT_TOKEN: res.body.token
}

settings {
  encodeUrl: true
}
